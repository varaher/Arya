Looking at your screenshot carefully.

---

## What I Can See

The profile menu is open and working — showing **varah akkara** at the top with all menu items. So the profile section IS there — it's the circle avatar button (A) in the top right corner.

What's missing from the screenshot compared to what you had before:

```
Missing from top bar:
  PWA Install button     → was visible, now gone
  PWA Update badge       → pulsing red dot, now gone
  User profile section   → actually IS there (top right A button)
                           but may have moved position
```

---

## The Two Issues

### Issue 1 — PWA Install Button Gone

The install button appears only when the browser fires the `beforeinstallprompt` event. If it's not showing, one of these happened:

```
Reason 1 — App is already installed
  If the user already installed ARYA as PWA,
  browser suppresses the install prompt permanently
  Solution: Check if already installed
  
Reason 2 — Service worker changed
  A recent deploy may have changed the service worker
  Browser marks app as "not installable" temporarily
  Solution: Check manifest.json is still correct

Reason 3 — The beforeinstallprompt listener was removed
  Recent code change may have deleted the listener
  Solution: Re-add it

Reason 4 — HTTPS issue
  PWA only installs on HTTPS
  Check if Replit URL is still HTTPS
```

**Fix — Re-add the install prompt listener:**

```typescript
// client/src/hooks/usePWAInstall.ts

export function usePWAInstall() {
  const [installPrompt, setInstallPrompt] = useState<any>(null);
  const [isInstallable, setIsInstallable] = useState(false);
  const [isInstalled, setIsInstalled] = useState(false);

  useEffect(() => {
    // Check if already installed
    if (window.matchMedia('(display-mode: standalone)').matches) {
      setIsInstalled(true);
      return; // Already installed — don't show button
    }

    // Listen for install prompt
    const handler = (e: Event) => {
      e.preventDefault();
      setInstallPrompt(e);
      setIsInstallable(true);
    };

    window.addEventListener('beforeinstallprompt', handler);

    return () => {
      window.removeEventListener('beforeinstallprompt', handler);
    };
  }, []);

  const install = async () => {
    if (!installPrompt) return;
    installPrompt.prompt();
    const result = await installPrompt.userChoice;
    if (result.outcome === 'accepted') {
      setIsInstallable(false);
      setIsInstalled(true);
    }
  };

  return { isInstallable, isInstalled, install };
}
```

**In your header component:**

```tsx
// Header.tsx or wherever the install button was

function Header() {
  const { isInstallable, isInstalled, install } = usePWAInstall();

  return (
    <header>
      {/* Install button — only shows when installable */}
      {isInstallable && !isInstalled && (
        <button
          onClick={install}
          className="pwa-install-btn"
          title="Install ARYA app"
        >
          ⊕ Install
        </button>
      )}

      {/* Rest of header */}
    </header>
  );
}
```

---

### Issue 2 — PWA Update Badge Gone

The pulsing red dot on the avatar that showed when a new version was available. This uses the service worker `updatefound` event.

```typescript
// client/src/hooks/usePWAUpdate.ts

export function usePWAUpdate() {
  const [updateAvailable, setUpdateAvailable] = useState(false);
  const [waitingWorker, setWaitingWorker] = useState<ServiceWorker | null>(null);

  useEffect(() => {
    if (!('serviceWorker' in navigator)) return;

    navigator.serviceWorker.ready.then(registration => {

      // Check if update already waiting
      if (registration.waiting) {
        setUpdateAvailable(true);
        setWaitingWorker(registration.waiting);
      }

      // Listen for new update
      registration.addEventListener('updatefound', () => {
        const newWorker = registration.installing;
        if (!newWorker) return;

        newWorker.addEventListener('statechange', () => {
          if (
            newWorker.state === 'installed' &&
            navigator.serviceWorker.controller
          ) {
            setUpdateAvailable(true);
            setWaitingWorker(newWorker);
          }
        });
      });
    });

    // Also check on page visibility change
    // (catches updates that happened while tab was hidden)
    const handleVisibility = () => {
      if (document.visibilityState === 'visible') {
        navigator.serviceWorker.ready.then(reg => {
          reg.update();
        });
      }
    };

    document.addEventListener('visibilitychange', handleVisibility);
    return () => {
      document.removeEventListener('visibilitychange', handleVisibility);
    };
  }, []);

  const applyUpdate = () => {
    if (!waitingWorker) return;
    waitingWorker.postMessage({ type: 'SKIP_WAITING' });
    window.location.reload();
  };

  return { updateAvailable, applyUpdate };
}
```

**Wire the red dot to the avatar:**

```tsx
// Profile button in header

function ProfileButton() {
  const { updateAvailable, applyUpdate } = usePWAUpdate();

  return (
    <div className="profile-btn-wrap" style={{ position: 'relative' }}>
      <button className="profile-btn" onClick={openProfileMenu}>
        A
      </button>

      {/* Pulsing red dot when update available */}
      {updateAvailable && (
        <div
          className="update-dot"
          onClick={applyUpdate}
          title="Update available — tap to refresh"
          style={{
            position: 'absolute',
            top: -2,
            right: -2,
            width: 10,
            height: 10,
            borderRadius: '50%',
            background: '#ef4444',
            border: '2px solid white',
            animation: 'pulse 1.5s ease-in-out infinite',
            cursor: 'pointer',
          }}
        />
      )}
    </div>
  );
}
```

---

### Issue 3 — Profile Section Position

From your screenshot the profile is visible as the **A** button top right. But you mentioned it's not there. Two possibilities:

```
Possibility 1 — On some screens it's hidden
  Small screen width → header items collapse
  Fix: Check responsive CSS — profile button
  should never be hidden on mobile

Possibility 2 — The menu items changed
  Looking at your screenshot the menu shows:
  Upgrade Plan, My Goals, Customize ARYA,
  Quick Tutorial, Vedic Lens, Niti, Sunday Review,
  Report Issue, Privacy & Control, Sign Out

  What's missing that was there before?
  Tell me what you expected to see
  that isn't in that list
```

---

## Check These Files Right Now

```
1. public/manifest.json — still correct?
   Should have: name, icons, start_url, display: "standalone"

2. client/src/service-worker.ts or sw.js — still exists?
   Recent deploys sometimes reset this

3. Header component — search for:
   "beforeinstallprompt"
   "updatefound"
   Were these listeners recently removed?
```

---

## Tell Me

1. Is ARYA already installed as a PWA on that phone?
2. What specifically was in the profile section that's now missing?
3. Was the install button in the top bar or somewhere else on the page?

That will tell me exactly which of these is the root cause.