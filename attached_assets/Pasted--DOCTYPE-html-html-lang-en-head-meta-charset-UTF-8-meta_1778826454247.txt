<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ARYA — Vedic Lens</title>
<link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:ital,wght@0,400;0,700;1,400&family=Nunito:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0d0b0f;
    --surface: #16121e;
    --surface2: #1e1928;
    --border: #2a2438;
    --border2: #352d48;
    --indigo: #7c6aff;
    --indigo-dim: #4a3fa0;
    --saffron: #f5a623;
    --saffron-dim: #a06a10;
    --rose: #e87070;
    --teal: #5ecfb0;
    --text: #ede8f5;
    --text-dim: #8a80a0;
    --text-muted: #3d3550;
    --star: #f5e642;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'Nunito', sans-serif;
    font-weight: 400;
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* Star field background */
  .stars {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 0;
    overflow: hidden;
  }
  .star {
    position: absolute;
    border-radius: 50%;
    background: white;
    animation: twinkle var(--dur, 3s) ease-in-out infinite;
    animation-delay: var(--delay, 0s);
  }
  @keyframes twinkle {
    0%, 100% { opacity: var(--min-op, 0.1); transform: scale(1); }
    50% { opacity: var(--max-op, 0.6); transform: scale(1.3); }
  }

  /* Mandala background glow */
  .mandala-bg {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 600px;
    height: 600px;
    border-radius: 50%;
    background: radial-gradient(ellipse,
      rgba(124,106,255,0.06) 0%,
      rgba(245,166,35,0.03) 40%,
      transparent 70%);
    pointer-events: none;
    z-index: 0;
    animation: breathe 8s ease-in-out infinite;
  }
  @keyframes breathe {
    0%, 100% { transform: translate(-50%,-50%) scale(1); opacity: 0.7; }
    50% { transform: translate(-50%,-50%) scale(1.1); opacity: 1; }
  }

  .screen {
    display: none;
    min-height: 100vh;
    position: relative;
    z-index: 1;
    animation: riseIn 0.6s cubic-bezier(0.16,1,0.3,1);
  }
  .screen.active { display: flex; flex-direction: column; }

  @keyframes riseIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ── NAV ── */
  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px 24px;
  }

  .nav-back {
    display: flex;
    align-items: center;
    gap: 6px;
    color: var(--text-dim);
    font-size: 13px;
    cursor: pointer;
    transition: color 0.2s;
  }
  .nav-back:hover { color: var(--saffron); }

  .nav-badge {
    font-size: 10px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--indigo);
    background: rgba(124,106,255,0.1);
    border: 1px solid rgba(124,106,255,0.2);
    padding: 4px 10px;
    border-radius: 20px;
  }

  .nav-skip {
    font-size: 13px;
    color: var(--text-muted);
    cursor: pointer;
    transition: color 0.2s;
  }
  .nav-skip:hover { color: var(--text-dim); }

  /* ── PROGRESS DOTS ── */
  .progress {
    display: flex;
    gap: 6px;
    justify-content: center;
    padding: 0 0 24px;
  }
  .prog-dot {
    width: 6px; height: 6px;
    border-radius: 3px;
    background: var(--border2);
    transition: all 0.3s;
  }
  .prog-dot.active {
    width: 20px;
    background: var(--indigo);
  }
  .prog-dot.done { background: var(--indigo-dim); }

  /* ── CONTENT ── */
  .content {
    flex: 1;
    padding: 8px 24px 40px;
    max-width: 460px;
    margin: 0 auto;
    width: 100%;
  }

  /* ── HERO ── */
  .hero-symbol {
    text-align: center;
    margin-bottom: 20px;
  }

  .symbol-ring {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 90px; height: 90px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(245,166,35,0.15), rgba(124,106,255,0.08));
    border: 1px solid rgba(245,166,35,0.2);
    font-size: 42px;
    position: relative;
    animation: spinSlow 20s linear infinite;
  }
  @keyframes spinSlow {
    from { box-shadow: 0 0 30px rgba(245,166,35,0.1), inset 0 0 20px rgba(124,106,255,0.05); }
    50% { box-shadow: 0 0 50px rgba(245,166,35,0.2), inset 0 0 30px rgba(124,106,255,0.1); }
    to { box-shadow: 0 0 30px rgba(245,166,35,0.1), inset 0 0 20px rgba(124,106,255,0.05); }
  }

  .eyebrow {
    font-size: 10px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--saffron-dim);
    text-align: center;
    margin-bottom: 10px;
  }

  h1 {
    font-family: 'Libre Baskerville', serif;
    font-size: 30px;
    font-weight: 400;
    line-height: 1.25;
    color: var(--text);
    text-align: center;
    margin-bottom: 10px;
  }
  h1 em {
    font-style: italic;
    color: var(--saffron);
  }

  .subtitle {
    font-size: 14px;
    color: var(--text-dim);
    line-height: 1.75;
    text-align: center;
    margin-bottom: 32px;
  }

  /* ── RASHI GRID ── */
  .rashi-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 8px;
    margin-bottom: 24px;
  }

  .rashi-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 12px 8px;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s;
    position: relative;
  }
  .rashi-card:hover {
    border-color: rgba(124,106,255,0.3);
    background: var(--surface2);
  }
  .rashi-card.selected {
    border-color: var(--indigo);
    background: rgba(124,106,255,0.1);
  }
  .rashi-card.selected::after {
    content: '✓';
    position: absolute;
    top: 4px; right: 6px;
    font-size: 9px;
    color: var(--indigo);
  }
  .rashi-symbol { font-size: 22px; margin-bottom: 4px; }
  .rashi-name { font-size: 10px; color: var(--text-dim); letter-spacing: 0.05em; }
  .rashi-english { font-size: 9px; color: var(--text-muted); margin-top: 2px; }

  /* ── BIRTH FIELDS ── */
  .field-group { display: flex; flex-direction: column; gap: 10px; margin-bottom: 24px; }

  .field-label {
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 4px;
  }

  .field-input {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 14px 16px;
    color: var(--text);
    font-family: 'Nunito', sans-serif;
    font-size: 14px;
    outline: none;
    transition: border-color 0.2s;
    width: 100%;
  }
  .field-input:focus { border-color: rgba(124,106,255,0.4); }
  .field-input::placeholder { color: var(--text-muted); }

  .field-row { display: flex; gap: 10px; }
  .field-row .field-wrap { flex: 1; }

  /* ── TIME APPROXIMATE ── */
  .time-approx {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 8px;
    margin-top: 6px;
  }
  .time-chip {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 10px 6px;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s;
    font-size: 11px;
    color: var(--text-dim);
  }
  .time-chip .time-emoji { font-size: 18px; display: block; margin-bottom: 4px; }
  .time-chip:hover { border-color: rgba(245,166,35,0.3); }
  .time-chip.selected {
    border-color: var(--saffron);
    background: rgba(245,166,35,0.08);
    color: var(--text);
  }

  /* ── FEATURE PREVIEW CARDS ── */
  .preview-list { display: flex; flex-direction: column; gap: 10px; margin-bottom: 28px; }

  .preview-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 14px;
    padding: 16px;
    display: flex;
    gap: 14px;
    align-items: flex-start;
    transition: border-color 0.2s;
  }
  .preview-card:hover { border-color: var(--border2); }

  .preview-icon {
    width: 40px; height: 40px;
    border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    font-size: 20px;
    flex-shrink: 0;
  }
  .icon-saffron { background: rgba(245,166,35,0.1); border: 1px solid rgba(245,166,35,0.15); }
  .icon-indigo { background: rgba(124,106,255,0.1); border: 1px solid rgba(124,106,255,0.15); }
  .icon-teal { background: rgba(94,207,176,0.1); border: 1px solid rgba(94,207,176,0.15); }
  .icon-rose { background: rgba(232,112,112,0.1); border: 1px solid rgba(232,112,112,0.15); }

  .preview-text-title { font-size: 14px; font-weight: 600; color: var(--text); margin-bottom: 3px; }
  .preview-text-desc { font-size: 12px; color: var(--text-dim); line-height: 1.6; }

  /* ── DISCLAIMER ── */
  .disclaimer {
    background: rgba(124,106,255,0.04);
    border: 1px solid rgba(124,106,255,0.12);
    border-radius: 12px;
    padding: 14px 16px;
    font-size: 12px;
    color: var(--text-dim);
    line-height: 1.7;
    display: flex;
    gap: 10px;
    margin-bottom: 24px;
  }

  /* ── BUTTONS ── */
  .btn {
    width: 100%;
    padding: 16px;
    border-radius: 14px;
    border: none;
    font-family: 'Nunito', sans-serif;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
    letter-spacing: 0.01em;
  }
  .btn-primary {
    background: linear-gradient(135deg, var(--indigo) 0%, #9b6aff 100%);
    color: white;
    box-shadow: 0 4px 20px rgba(124,106,255,0.3);
  }
  .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 6px 24px rgba(124,106,255,0.4); }

  .btn-saffron {
    background: linear-gradient(135deg, var(--saffron) 0%, #f5c842 100%);
    color: #1a1200;
    box-shadow: 0 4px 20px rgba(245,166,35,0.3);
  }
  .btn-saffron:hover { transform: translateY(-1px); box-shadow: 0 6px 24px rgba(245,166,35,0.4); }

  .btn-ghost {
    background: transparent;
    color: var(--text-dim);
    border: 1px solid var(--border);
    margin-top: 10px;
  }
  .btn-ghost:hover { border-color: var(--border2); color: var(--text); }

  /* ════════════════════════
     MORNING BRIEFING SCREEN
  ════════════════════════ */
  .briefing-header {
    background: linear-gradient(180deg, rgba(245,166,35,0.08) 0%, transparent 100%);
    border-bottom: 1px solid var(--border);
    padding: 24px 24px 20px;
  }

  .briefing-meta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 16px;
  }
  .briefing-date { font-size: 12px; color: var(--text-muted); letter-spacing: 0.08em; }
  .briefing-name {
    font-family: 'Libre Baskerville', serif;
    font-size: 22px;
    color: var(--text);
    margin-bottom: 4px;
  }
  .briefing-name em { font-style: italic; color: var(--saffron); }
  .briefing-sub { font-size: 13px; color: var(--text-dim); }

  /* Planetary bar */
  .planet-bar {
    display: flex;
    gap: 8px;
    overflow-x: auto;
    padding-bottom: 4px;
    scrollbar-width: none;
  }
  .planet-bar::-webkit-scrollbar { display: none; }

  .planet-pill {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 6px 12px;
    border-radius: 20px;
    border: 1px solid var(--border);
    background: var(--surface);
    white-space: nowrap;
    font-size: 12px;
    color: var(--text-dim);
    flex-shrink: 0;
  }
  .planet-pill.good { border-color: rgba(94,207,176,0.3); color: var(--teal); background: rgba(94,207,176,0.06); }
  .planet-pill.caution { border-color: rgba(245,166,35,0.3); color: var(--saffron); background: rgba(245,166,35,0.06); }
  .planet-pill.watch { border-color: rgba(232,112,112,0.3); color: var(--rose); background: rgba(232,112,112,0.06); }

  /* ── BRIEFING SECTIONS ── */
  .briefing-body { padding: 0 24px 40px; max-width: 460px; margin: 0 auto; width: 100%; }

  .section-gap { height: 20px; }

  .briefing-section {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 16px;
    overflow: hidden;
    margin-bottom: 14px;
  }

  .section-head {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 14px 16px;
    border-bottom: 1px solid var(--border);
  }
  .section-head-icon { font-size: 18px; }
  .section-head-title {
    font-size: 11px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--text-muted);
    font-weight: 600;
  }
  .section-head-badge {
    margin-left: auto;
    font-size: 9px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 3px 8px;
    border-radius: 10px;
  }
  .badge-vedic {
    color: var(--saffron);
    background: rgba(245,166,35,0.08);
    border: 1px solid rgba(245,166,35,0.2);
  }
  .badge-personal {
    color: var(--indigo);
    background: rgba(124,106,255,0.08);
    border: 1px solid rgba(124,106,255,0.2);
  }

  .section-body { padding: 16px; }

  /* ── VEDIC INSIGHT CARD ── */
  .vedic-card {
    border-radius: 12px;
    padding: 16px;
    margin-bottom: 10px;
    position: relative;
    overflow: hidden;
  }
  .vedic-card::before {
    content: '';
    position: absolute;
    left: 0; top: 0; bottom: 0;
    width: 3px;
    border-radius: 3px 0 0 3px;
  }
  .vedic-card.tone-good { background: rgba(94,207,176,0.06); border: 1px solid rgba(94,207,176,0.15); }
  .vedic-card.tone-good::before { background: var(--teal); }
  .vedic-card.tone-caution { background: rgba(245,166,35,0.06); border: 1px solid rgba(245,166,35,0.15); }
  .vedic-card.tone-caution::before { background: var(--saffron); }
  .vedic-card.tone-watch { background: rgba(232,112,112,0.06); border: 1px solid rgba(232,112,112,0.15); }
  .vedic-card.tone-watch::before { background: var(--rose); }

  .vedic-card-top {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 8px;
  }
  .vedic-card-label { font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase; }
  .vedic-card-label.good { color: var(--teal); }
  .vedic-card-label.caution { color: var(--saffron); }
  .vedic-card-label.watch { color: var(--rose); }

  .vedic-card-text {
    font-size: 14px;
    color: var(--text);
    line-height: 1.7;
    font-style: italic;
    font-family: 'Libre Baskerville', serif;
    font-weight: 400;
  }

  .vedic-card-source {
    font-size: 10px;
    color: var(--text-muted);
    margin-top: 8px;
    font-family: 'Nunito', sans-serif;
    font-style: normal;
  }

  /* ── TODAY'S GUIDANCE ── */
  .guidance-row {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 10px 0;
    border-bottom: 1px solid var(--border);
  }
  .guidance-row:last-child { border-bottom: none; padding-bottom: 0; }
  .guidance-emoji { font-size: 20px; flex-shrink: 0; margin-top: 2px; }
  .guidance-label { font-size: 11px; color: var(--text-muted); margin-bottom: 3px; letter-spacing: 0.05em; }
  .guidance-text { font-size: 13px; color: var(--text); line-height: 1.6; }
  .guidance-highlight { color: var(--saffron); font-weight: 600; }

  /* ── PERSONAL ARYA INSIGHT ── */
  .arya-insight {
    background: rgba(124,106,255,0.06);
    border: 1px solid rgba(124,106,255,0.15);
    border-radius: 14px;
    padding: 16px;
    margin-bottom: 14px;
  }
  .arya-insight-top {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 10px;
  }
  .arya-avatar {
    width: 28px; height: 28px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--indigo), #9b6aff);
    display: flex; align-items: center; justify-content: center;
    font-size: 14px;
  }
  .arya-insight-label { font-size: 11px; color: var(--indigo); letter-spacing: 0.08em; }
  .arya-insight-text {
    font-size: 14px;
    color: var(--text);
    line-height: 1.75;
  }
  .arya-insight-text strong { color: var(--saffron); font-weight: 600; }

  /* ── MUHURAT STRIP ── */
  .muhurat-strip {
    background: linear-gradient(135deg, rgba(245,166,35,0.08), rgba(124,106,255,0.08));
    border: 1px solid rgba(245,166,35,0.2);
    border-radius: 12px;
    padding: 14px 16px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 14px;
  }
  .muhurat-left { display: flex; align-items: center; gap: 10px; }
  .muhurat-icon { font-size: 22px; }
  .muhurat-label { font-size: 11px; color: var(--text-muted); margin-bottom: 2px; }
  .muhurat-time { font-size: 16px; color: var(--saffron); font-weight: 600; letter-spacing: 0.05em; }
  .muhurat-right { font-size: 11px; color: var(--text-dim); text-align: right; }

  /* ── GOALS TODAY ── */
  .goal-item {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 11px 0;
    border-bottom: 1px solid var(--border);
    cursor: pointer;
  }
  .goal-item:last-child { border-bottom: none; }
  .goal-check {
    width: 20px; height: 20px;
    border-radius: 6px;
    border: 1.5px solid var(--border2);
    flex-shrink: 0;
    transition: all 0.2s;
    display: flex; align-items: center; justify-content: center;
  }
  .goal-item.done .goal-check {
    background: var(--teal);
    border-color: var(--teal);
    color: white;
    font-size: 11px;
  }
  .goal-text { font-size: 13px; color: var(--text); flex: 1; }
  .goal-item.done .goal-text { text-decoration: line-through; color: var(--text-dim); }
  .goal-streak {
    font-size: 11px;
    color: var(--saffron);
    display: flex;
    align-items: center;
    gap: 3px;
  }

  /* ── TAB BAR ── */
  .tab-bar {
    position: sticky;
    bottom: 0;
    background: rgba(13,11,15,0.95);
    backdrop-filter: blur(20px);
    border-top: 1px solid var(--border);
    display: flex;
    padding: 10px 0 20px;
    z-index: 10;
  }
  .tab-item {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    cursor: pointer;
    opacity: 0.4;
    transition: opacity 0.2s;
    font-size: 10px;
    color: var(--text-dim);
  }
  .tab-item.active { opacity: 1; color: var(--indigo); }
  .tab-item-icon { font-size: 22px; }

</style>
</head>
<body>

<!-- Star field -->
<div class="stars" id="stars"></div>
<div class="mandala-bg"></div>

<!-- ════════════════════════════════════
     SCREEN 1 — INTRO / WHAT IS THIS
════════════════════════════════════ -->
<div class="screen active" id="s-intro">
  <div class="nav">
    <div class="nav-back" onclick="go('s-briefing')" style="font-size:12px; color:var(--text-muted)">Skip for now →</div>
    <div class="nav-badge">✦ Vedic Lens</div>
    <div style="width:60px"></div>
  </div>

  <div class="progress">
    <div class="prog-dot active"></div>
    <div class="prog-dot"></div>
    <div class="prog-dot"></div>
    <div class="prog-dot"></div>
  </div>

  <div class="content">
    <div class="hero-symbol">
      <div class="symbol-ring">🪐</div>
    </div>

    <div class="eyebrow">Ancient Wisdom · Modern Life</div>
    <h1>Meet your<br><em>Vedic Lens</em></h1>
    <p class="subtitle">India's oldest wisdom tradition — Jyotish — translated into plain, practical guidance for your everyday life. No jargon. No fear. Just clarity.</p>

    <div class="preview-list">
      <div class="preview-card">
        <div class="preview-icon icon-saffron">🌅</div>
        <div>
          <div class="preview-text-title">Morning Cosmic Briefing</div>
          <div class="preview-text-desc">What the day holds for you — in plain language. Not "Mercury is combust." Just "double-check what you send today."</div>
        </div>
      </div>
      <div class="preview-card">
        <div class="preview-icon icon-indigo">⏰</div>
        <div>
          <div class="preview-text-title">Auspicious Timing (Muhurat)</div>
          <div class="preview-text-desc">The best window today for starting something new, having an important conversation, or signing a document.</div>
        </div>
      </div>
      <div class="preview-card">
        <div class="preview-icon icon-teal">🔮</div>
        <div>
          <div class="preview-text-title">Your Personal Dasha Period</div>
          <div class="preview-text-desc">The bigger chapter your life is in right now — and what that means for your energy, decisions and growth.</div>
        </div>
      </div>
      <div class="preview-card">
        <div class="preview-icon icon-rose">💡</div>
        <div>
          <div class="preview-text-title">Blended with Your Real Life</div>
          <div class="preview-text-desc">ARYA connects cosmic context with what's actually happening in your life — your goals, your mood, your week.</div>
        </div>
      </div>
    </div>

    <div class="disclaimer">
      <div style="font-size:16px; flex-shrink:0">🙏</div>
      <div>Vedic Lens offers perspective and reflection — not prediction. Think of it as a wise elder's view, not a fortune teller's. You always decide.</div>
    </div>

    <button class="btn btn-saffron" onclick="go('s-rashi')">Begin setup</button>
    <button class="btn btn-ghost" onclick="go('s-briefing')">Skip — show me the briefing</button>
  </div>
</div>


<!-- ════════════════════════════════════
     SCREEN 2 — RASHI SELECTION
════════════════════════════════════ -->
<div class="screen" id="s-rashi">
  <div class="nav">
    <div class="nav-back" onclick="go('s-intro')">← Back</div>
    <div class="nav-badge">✦ Vedic Lens</div>
    <div class="nav-skip" onclick="go('s-birth')">Skip</div>
  </div>

  <div class="progress">
    <div class="prog-dot done"></div>
    <div class="prog-dot active"></div>
    <div class="prog-dot"></div>
    <div class="prog-dot"></div>
  </div>

  <div class="content">
    <div class="eyebrow">Step 1 of 3</div>
    <h1>What is your<br><em>Rashi?</em></h1>
    <p class="subtitle">Your Moon sign in Vedic astrology. If you don't know it — no problem, enter your birth details next and ARYA will calculate it.</p>

    <div class="rashi-grid">
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♈</div>
        <div class="rashi-name">Mesh</div>
        <div class="rashi-english">Aries</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♉</div>
        <div class="rashi-name">Vrishabh</div>
        <div class="rashi-english">Taurus</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♊</div>
        <div class="rashi-name">Mithun</div>
        <div class="rashi-english">Gemini</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♋</div>
        <div class="rashi-name">Kark</div>
        <div class="rashi-english">Cancer</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♌</div>
        <div class="rashi-name">Simha</div>
        <div class="rashi-english">Leo</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♍</div>
        <div class="rashi-name">Kanya</div>
        <div class="rashi-english">Virgo</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♎</div>
        <div class="rashi-name">Tula</div>
        <div class="rashi-english">Libra</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♏</div>
        <div class="rashi-name">Vrishchik</div>
        <div class="rashi-english">Scorpio</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♐</div>
        <div class="rashi-name">Dhanu</div>
        <div class="rashi-english">Sagittarius</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♑</div>
        <div class="rashi-name">Makar</div>
        <div class="rashi-english">Capricorn</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♒</div>
        <div class="rashi-name">Kumbh</div>
        <div class="rashi-english">Aquarius</div>
      </div>
      <div class="rashi-card" onclick="selectRashi(this)">
        <div class="rashi-symbol">♓</div>
        <div class="rashi-name">Meen</div>
        <div class="rashi-english">Pisces</div>
      </div>
    </div>

    <button class="btn btn-primary" onclick="go('s-birth')">Continue →</button>
    <button class="btn btn-ghost" onclick="go('s-birth')">I don't know my Rashi</button>
  </div>
</div>


<!-- ════════════════════════════════════
     SCREEN 3 — BIRTH DETAILS
════════════════════════════════════ -->
<div class="screen" id="s-birth">
  <div class="nav">
    <div class="nav-back" onclick="go('s-rashi')">← Back</div>
    <div class="nav-badge">✦ Vedic Lens</div>
    <div class="nav-skip" onclick="go('s-ready')">Skip</div>
  </div>

  <div class="progress">
    <div class="prog-dot done"></div>
    <div class="prog-dot done"></div>
    <div class="prog-dot active"></div>
    <div class="prog-dot"></div>
  </div>

  <div class="content">
    <div class="eyebrow">Step 2 of 3</div>
    <h1>Your birth<br><em>details</em></h1>
    <p class="subtitle">This lets ARYA calculate your personal Kundli — your exact cosmic fingerprint. More accurate than any generic horoscope.</p>

    <div class="field-group">
      <div class="field-wrap">
        <div class="field-label">Date of birth</div>
        <input class="field-input" type="date" placeholder="DD / MM / YYYY">
      </div>

      <div class="field-wrap">
        <div class="field-label">Place of birth</div>
        <input class="field-input" type="text" placeholder="e.g. Chennai, Tamil Nadu">
      </div>

      <div class="field-wrap">
        <div class="field-label">Time of birth — even approximate is fine</div>
        <div class="time-approx">
          <div class="time-chip" onclick="selectTime(this)">
            <span class="time-emoji">🌅</span>Morning
          </div>
          <div class="time-chip" onclick="selectTime(this)">
            <span class="time-emoji">☀️</span>Afternoon
          </div>
          <div class="time-chip" onclick="selectTime(this)">
            <span class="time-emoji">🌆</span>Evening
          </div>
          <div class="time-chip" onclick="selectTime(this)">
            <span class="time-emoji">🌙</span>Night
          </div>
        </div>
        <div style="margin-top: 10px;">
          <input class="field-input" type="time" placeholder="Exact time if known">
        </div>
      </div>
    </div>

    <div class="disclaimer">
      <div style="font-size:16px; flex-shrink:0">🔒</div>
      <div>Your birth details are stored only on your device and used solely to generate your Kundli. They are never shared. You can delete them anytime via Right to Forget.</div>
    </div>

    <button class="btn btn-primary" onclick="go('s-ready')">Calculate my Kundli →</button>
    <button class="btn btn-ghost" onclick="go('s-ready')">Skip — use just my Rashi</button>
  </div>
</div>


<!-- ════════════════════════════════════
     SCREEN 4 — READY
════════════════════════════════════ -->
<div class="screen" id="s-ready">
  <div class="nav">
    <div class="nav-back" onclick="go('s-birth')">← Back</div>
    <div class="nav-badge">✦ Vedic Lens</div>
    <div style="width:60px"></div>
  </div>

  <div class="progress">
    <div class="prog-dot done"></div>
    <div class="prog-dot done"></div>
    <div class="prog-dot done"></div>
    <div class="prog-dot active"></div>
  </div>

  <div class="content" style="text-align:center; display:flex; flex-direction:column; align-items:center; justify-content:center; min-height:70vh;">
    <div class="symbol-ring" style="margin-bottom:24px; width:100px; height:100px; font-size:48px;">✨</div>
    <div class="eyebrow">Your Kundli is ready</div>
    <h1>Vedic Lens<br><em>is active.</em></h1>
    <p class="subtitle" style="max-width:300px; margin:0 auto 32px;">Every morning briefing now includes your personal cosmic context — in plain, simple language. No jargon ever.</p>

    <div style="background:var(--surface); border:1px solid var(--border); border-radius:16px; padding:18px; width:100%; max-width:320px; text-align:left; margin-bottom:28px;">
      <div style="font-size:11px; color:var(--text-muted); letter-spacing:0.12em; text-transform:uppercase; margin-bottom:12px;">Your cosmic profile</div>
      <div style="display:flex; flex-direction:column; gap:8px;">
        <div style="display:flex; justify-content:space-between; font-size:13px;">
          <span style="color:var(--text-dim)">Rashi (Moon sign)</span>
          <span style="color:var(--saffron)">Vrishchik ♏</span>
        </div>
        <div style="display:flex; justify-content:space-between; font-size:13px;">
          <span style="color:var(--text-dim)">Current Dasha</span>
          <span style="color:var(--text)">Jupiter · 4 yrs left</span>
        </div>
        <div style="display:flex; justify-content:space-between; font-size:13px;">
          <span style="color:var(--text-dim)">Nakshatra</span>
          <span style="color:var(--text)">Jyeshtha</span>
        </div>
        <div style="display:flex; justify-content:space-between; font-size:13px;">
          <span style="color:var(--text-dim)">Source</span>
          <span style="color:var(--text-dim)">Brihat Parashara Hora</span>
        </div>
      </div>
    </div>

    <button class="btn btn-saffron" onclick="go('s-briefing')" style="max-width:320px;">See today's briefing →</button>
  </div>
</div>


<!-- ════════════════════════════════════
     SCREEN 5 — MORNING BRIEFING
════════════════════════════════════ -->
<div class="screen" id="s-briefing">
  <div class="briefing-header">
    <div class="briefing-meta">
      <div class="briefing-date">FRIDAY · 15 MAY 2026 · 7:14 AM</div>
      <div style="font-size:20px">🌤️</div>
    </div>
    <div class="briefing-name">Good morning, <em>Arjun.</em></div>
    <div class="briefing-sub">Here's what today looks like — for you, specifically.</div>

    <div style="height:16px"></div>

    <!-- Planetary pill bar — plain language -->
    <div class="planet-bar">
      <div class="planet-pill good">☿ Good for communication</div>
      <div class="planet-pill caution">♄ Be patient with delays</div>
      <div class="planet-pill good">♃ Decisions feel clearer</div>
      <div class="planet-pill watch">♂ Avoid arguments after 6pm</div>
    </div>
  </div>

  <div class="briefing-body">
    <div class="section-gap"></div>

    <!-- ARYA's Personal Insight -->
    <div class="arya-insight">
      <div class="arya-insight-top">
        <div class="arya-avatar">A</div>
        <div class="arya-insight-label">ARYA · Blended Insight</div>
      </div>
      <div class="arya-insight-text">
        You've been more focused than usual this week — your mood logs show it. <strong>Today's cosmic energy supports that clarity.</strong> Jupiter is in a strong position for Vrishchik right now, which means your instincts about the job decision are probably right. Trust what you already know.
      </div>
    </div>

    <!-- Auspicious timing -->
    <div class="muhurat-strip">
      <div class="muhurat-left">
        <div class="muhurat-icon">⏰</div>
        <div>
          <div class="muhurat-label">Best window today</div>
          <div class="muhurat-time">10:15 AM – 11:45 AM</div>
        </div>
      </div>
      <div class="muhurat-right">For starting<br>something new</div>
    </div>

    <!-- Vedic Insights Section -->
    <div class="briefing-section">
      <div class="section-head">
        <div class="section-head-icon">🪐</div>
        <div class="section-head-title">Today's Cosmic Guidance</div>
        <div class="section-head-badge badge-vedic">Vedic Lens</div>
      </div>
      <div class="section-body">

        <div class="vedic-card tone-good">
          <div class="vedic-card-top">
            <span style="font-size:14px">✦</span>
            <div class="vedic-card-label good">Strength today</div>
          </div>
          <div class="vedic-card-text">"Your thinking is unusually clear today. If you've been sitting on a decision — this is a good day to make it."</div>
          <div class="vedic-card-source">Jupiter influence · Vrishchik Rashi · Brihat Parashara Hora Shastra</div>
        </div>

        <div class="vedic-card tone-caution">
          <div class="vedic-card-top">
            <span style="font-size:14px">◈</span>
            <div class="vedic-card-label caution">Handle with care</div>
          </div>
          <div class="vedic-card-text">"Be a little more patient than usual with people around you today — especially in the evening. Small misunderstandings can feel bigger than they are."</div>
          <div class="vedic-card-source">Saturn transit · Phaladeepika reference</div>
        </div>

        <div class="guidance-row">
          <div class="guidance-emoji">💰</div>
          <div>
            <div class="guidance-label">Money & finances</div>
            <div class="guidance-text">Fine for routine expenses. <span class="guidance-highlight">Avoid big financial commitments</span> — wait until next week when the energy settles.</div>
          </div>
        </div>

        <div class="guidance-row">
          <div class="guidance-emoji">❤️</div>
          <div>
            <div class="guidance-label">Relationships</div>
            <div class="guidance-text">A good day to say something kind that you've been meaning to say. Small gestures land well today.</div>
          </div>
        </div>

        <div class="guidance-row">
          <div class="guidance-emoji">🏃</div>
          <div>
            <div class="guidance-label">Body & energy</div>
            <div class="guidance-text">Energy is moderate. Don't push too hard physically. Rest if you feel it — your body is asking for it this week.</div>
          </div>
        </div>

      </div>
    </div>

    <!-- Your Goals Today -->
    <div class="briefing-section">
      <div class="section-head">
        <div class="section-head-icon">🎯</div>
        <div class="section-head-title">Your Focus Today</div>
        <div class="section-head-badge badge-personal">Personal</div>
      </div>
      <div class="section-body">
        <div class="goal-item done" onclick="toggleGoal(this)">
          <div class="goal-check">✓</div>
          <div class="goal-text">Morning walk — 30 mins</div>
          <div class="goal-streak">🔥 12</div>
        </div>
        <div class="goal-item" onclick="toggleGoal(this)">
          <div class="goal-check"></div>
          <div class="goal-text">Read for 20 minutes</div>
          <div class="goal-streak">🔥 5</div>
        </div>
        <div class="goal-item" onclick="toggleGoal(this)">
          <div class="goal-check"></div>
          <div class="goal-text">Reply to Meera about the project</div>
          <div class="goal-streak"></div>
        </div>
        <div class="goal-item" onclick="toggleGoal(this)">
          <div class="goal-check"></div>
          <div class="goal-text">Meditate — even 5 minutes counts</div>
          <div class="goal-streak">🔥 3</div>
        </div>
      </div>
    </div>

    <!-- Weekly Dasha context -->
    <div class="briefing-section">
      <div class="section-head">
        <div class="section-head-icon">📖</div>
        <div class="section-head-title">Your Bigger Chapter</div>
        <div class="section-head-badge badge-vedic">Dasha Period</div>
      </div>
      <div class="section-body">
        <div class="vedic-card tone-good" style="margin-bottom:0">
          <div class="vedic-card-text">"You are in a Jupiter Dasha period — a chapter associated with growth, learning, and expansion. This is a good time in your life to take on more, to study, to lead. The energy supports long-term building, not quick wins."</div>
          <div class="vedic-card-source">Jupiter Mahadasha · ~4 years remaining · Saravali</div>
        </div>
      </div>
    </div>

    <div style="height:20px"></div>
  </div>

  <!-- Tab bar -->
  <div class="tab-bar">
    <div class="tab-item active">
      <div class="tab-item-icon">🌅</div>
      Today
    </div>
    <div class="tab-item" onclick="go('s-intro')">
      <div class="tab-item-icon">🪐</div>
      Vedic
    </div>
    <div class="tab-item">
      <div class="tab-item-icon">💬</div>
      Chat
    </div>
    <div class="tab-item">
      <div class="tab-item-icon">🎯</div>
      Goals
    </div>
    <div class="tab-item">
      <div class="tab-item-icon">👤</div>
      You
    </div>
  </div>
</div>


<script>
  // Generate stars
  const starsEl = document.getElementById('stars');
  for (let i = 0; i < 80; i++) {
    const s = document.createElement('div');
    s.className = 'star';
    const size = Math.random() * 2 + 0.5;
    s.style.cssText = `
      width:${size}px; height:${size}px;
      left:${Math.random()*100}%;
      top:${Math.random()*100}%;
      --dur:${2+Math.random()*4}s;
      --delay:${Math.random()*4}s;
      --min-op:${0.05+Math.random()*0.1};
      --max-op:${0.3+Math.random()*0.5};
    `;
    starsEl.appendChild(s);
  }

  function go(id) {
    document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
    document.getElementById(id).classList.add('active');
    window.scrollTo(0, 0);
  }

  function selectRashi(el) {
    document.querySelectorAll('.rashi-card').forEach(c => c.classList.remove('selected'));
    el.classList.add('selected');
  }

  function selectTime(el) {
    document.querySelectorAll('.time-chip').forEach(c => c.classList.remove('selected'));
    el.classList.add('selected');
  }

  function toggleGoal(el) {
    el.classList.toggle('done');
    const check = el.querySelector('.goal-check');
    check.textContent = el.classList.contains('done') ? '✓' : '';
  }
</script>
</body>
</html>
