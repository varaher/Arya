Looking at the screenshot carefully.

---

## Two Issues I Can See

### Issue 1 — PDF Not Reading
```
User uploaded PDF → 
"I wasn't able to extract text from that PDF.
It may be a scanned/image-only PDF."

BUT the image upload worked perfectly ✅
ARYA gave detailed medical analysis of the
lab report image — excellent response!

So: Images work ✅, PDFs fail ❌
```

### Issue 2 — Only One File At A Time
```
User wants to upload multiple files together
Currently: one file per message
Needed: multiple files in one message
```

---

## Why PDFs Fail

```
Two types of PDFs:
  Text PDF: Words stored as text → easy to read
  Scanned PDF: Photo inside PDF → needs OCR

Most medical reports in India are:
  Scanned PDFs (photographed paper) ❌
  ARYA can't read these currently

The image worked because:
  You converted PDF page to photo
  ARYA's vision read the image ✅
```

---

## Fix 1 — PDF with OCR Support

```typescript
// server/routes.ts — scan endpoint
// When PDF fails text extraction:
// Convert PDF pages to images → then read

// Current flow:
PDF upload → extract text → if empty → error ❌

// Better flow:
PDF upload → extract text
  → if text found → use text ✅
  → if empty (scanned) → convert to image
    → send to GPT vision ✅
    → works for all PDFs ✅
```

Tell developer:
```
In the PDF scan handler:
If PDF text extraction returns empty
  Use pdf-to-image or canvas to render
  each PDF page as an image
  Then send images to GPT-4o vision
  Same as how image upload works now

Library to use: pdf2pic or pdfjs-dist
Already in project likely
```

---

## Fix 2 — Multiple File Upload

```typescript
// client/src/pages/AryaChat.tsx
// Find the file input element

// Current — single file:
<input
  type="file"
  accept="image/*,.pdf"
  // no multiple attribute
/>

// Fix — add multiple:
<input
  type="file"
  accept="image/*,.pdf"
  multiple          // ← ADD THIS ONE WORD
  onChange={handleMultipleFiles}
/>

// Handle multiple files:
async function handleMultipleFiles(
  e: React.ChangeEvent<HTMLInputElement>
) {
  const files = Array.from(e.target.files || []);
  // Process each file
  for (const file of files) {
    await attachFile(file);
  }
}
```

---

## What ARYA Did Well ✅

Looking at the lab report analysis — this is excellent:

```
✅ Identified: Microbiology Aerobic Culture
✅ Found: Enterobacter species
✅ Detected: Multi-Drug Resistant (MDR)
✅ Explained: All antibiotics showing R (Resistant)
✅ Listed: Ampicillin, Ciprofloxacin, Amikacin,
           Imipenem, Meropenem all resistant
✅ Gave: Clinical significance
✅ Recommended: Infectious disease specialist
✅ Suggested: Combination therapy / last-resort antibiotics
✅ Added: Infection control measures warning

This is better than most junior doctors
would explain it. Very impressive.
```

---

## Tell Your Developer — Two Quick Fixes

```
Fix 1 — Scanned PDF support (2-3 hours)
  When PDF text extraction returns empty:
  Convert PDF pages to images using pdfjs-dist
  Send images to GPT-4o vision API
  Return analysis

Fix 2 — Multiple files (15 minutes)
  Add "multiple" attribute to file input
  Update handler to process array of files
  Show preview for each file
  Send all in one message
```

---

## After These Fixes

```
User can:
  Upload 5 lab reports at once ✅
  Upload scanned PDFs ✅
  Upload photos of reports ✅ (already works)
  Get detailed medical analysis ✅

ARYA becomes a proper
medical report analyzer —
very useful for a doctor like you.
```